import { Button, Section } from '../ui';

const GetStarted = () => {
    return (
        <Section>
            <h2 className='text-[42px] font-bold text-center'>
                Get started with US today
            </h2>
            <span className='text-textGray mb-4'>
                Make Money From Your Unwanted Staff.
            </span>
            <Button variant='primary'>Start Selling</Button>
        </Section>
    );
};

export default GetStarted;
